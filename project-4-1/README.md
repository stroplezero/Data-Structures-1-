유클리드 호제법으로 GCD를 구하는 알고리즘의 시간 복잡도는 가장 핵심적인 코드 한 줄이 최악의 경우, 몇 번 실행되는지 알아보는 것으로 계산할 수 있다.
알고리즘에서 가장 핵심적인 코드 한줄은 n을 대입했을 때 피보나치 수열에서 n번째 수를 구하는 F(n)을 구하는 코드이다. 
F(n)이 재귀적 방법으로 구현되었을 때, F(n)은 n이 1 증가할 때마다 자기 자신을 2번씩 추가로 더 호출하므로 O(2^n)의 시간 복잡도를 가진다. 
